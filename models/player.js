class Player {
  constructor(id, avatar, name, race, className, stats ){
    this.id = id;
    this.avatar = avatar;
    this.name = name;
    this.race = race;
    this.className = className;
    this.stats = stats;
    //this.users = users;
  }
};

export default Player;
