class Player {
  constructor(id, name, className, stats ){
    this.id = id;
    this.name = name;
    this.className = className;
    this.stats = stats;
    //this.users = users;
  }
};

export default Player;
